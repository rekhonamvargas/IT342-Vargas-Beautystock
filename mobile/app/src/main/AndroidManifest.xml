<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:name=".BeautyStockApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="BeautyStock"
        android:supportsRtl="true"
        android:theme="@style/Theme.BeautyStock"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.BeautyStock" />

        <activity
            android:name=".ui.xml.LandingActivity"
            android:exported="true"
            android:theme="@style/Theme.BeautyStock">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.xml.LoginActivity" android:exported="false" />
        <activity android:name=".ui.xml.RegisterActivity" android:exported="false" />
        <activity android:name=".ui.xml.DashboardActivity" android:exported="false" />
    </application>

</manifest>
